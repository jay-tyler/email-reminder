{% extends "base.jinja2" %}
{% block body %}
    {% for reminder in reminders %}
        <h2>{{ reminder.title }}</h2>
        <h3>{{ reminder.payload }}</h3>
        <p>{{ reminder.delivery_time }}</p>
    {% endfor %}
{% endblock %}